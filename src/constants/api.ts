import { API_PATH } from './config';

const AUTH_PATH = `${API_PATH}auth/`;
const INVENTORY_PATH = `${API_PATH}inventory/`;
const PRODUCT_PATH = `${API_PATH}product/`;
const ACTIVITY_PATH = `${API_PATH}activity/`;
const USER_PATH = `${API_PATH}user/`;
const PROFILE_PATH = `${API_PATH}profile/`;
const SALE_PATH = `${API_PATH}sale/`;
const REPORT_PATH = `${API_PATH}report/`;
// const INVENTORY_GET_PATH = `${INVENTORY_PATH}get`;
// const INVENTORY_ADD_PATH = `${INVENTORY_PATH}add`;
// const INVENTORY_UPDATE_PATH = `${INVENTORY_PATH}update`;
// const INVENTORY_REMOVE_PATH = `${INVENTORY_PATH}update`;

// const PRODUCT_GET_PATH = `${PRODUCT_PATH}get`;
// const PRODUCT_ADD_PATH = `${PRODUCT_PATH}add`;
// const PRODUCT_UPDATE_PATH = `${PRODUCT_PATH}update`;
// const PRODUCT_REMOVE_PATH = `${PRODUCT_PATH}update`;

// const ACTIVITY_GET_PATH = `${ACTIVITY_PATH}get`;

export {
	AUTH_PATH,
	INVENTORY_PATH,
	PRODUCT_PATH,
	ACTIVITY_PATH,
	USER_PATH,
	PROFILE_PATH,
	SALE_PATH,
	REPORT_PATH,
};
